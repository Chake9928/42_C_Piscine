#include "ft_abs.h"
#include <stdio.h>

int		main(void)
{
	int a = 42;
	int b = 0;
	int c = -24;

	printf("the absulute value of a is %d\n", ABS(a));
	printf("the absulute value of b is %d\n", ABS(b));
	printf("the absulute value of c is %d\n", ABS(c));

}
